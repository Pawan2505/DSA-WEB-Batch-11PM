#include<iostream>
using namespace std;

int main(){
    int num;

    // For loop with manual increment of num inside the loop body
    for(num = 1; num <= 100; ){
        cout << num << endl;  // Output each number
        num++;  // Manually increment num
    }
}
